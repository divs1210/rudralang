ns!(letfn,
  {} =>

  letfn is-even? := [n] =>
    cond(
      (n == 0) => true
      :else    => is-odd?((n - 1))
    )
  :and is-odd? := [n] =>
    cond(
      (n == 0) => false
      :else    => is-even?((n - 1))
    )

  defn!(main!, {}, _ =>
    println!(is-even?(100000))
  )
)