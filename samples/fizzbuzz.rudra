ns!(fizzbuzz,
  {} =>

  defn!(fizzbuzz, {}, [n] =>
    cond(
      zero?(mod(n, 15)) => "FizzBuzz"
      zero?(mod(n, 5))  => "Buzz"
      zero?(mod(n, 3))  => "Fizz"
      :else             => n
    )
  )

  defn!(main!, {}, args =>
    println!(fizzbuzz(string->number(nth(args, 1))))
  )
)
