ns!(fact,
  {} =>

  declare!(fact)

  defn!(fact, {}, [n] =>
    cond(
      equal?(n, 0) => 1,
      :else        => (n * fact((n - 1)))
    )
  )

  defn!(main!, {}, [_, n] =>
    println!(fact(string->number(n)))
  )
)