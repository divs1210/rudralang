ns!(fact,
  {} =>

  defn!(fact, {}, [n] =>
    cond(
      equal?(n, 0) => 1,
      :else        => mul(n, fact(sub(n, 1)))
    )
  )

  defn!(main!, {}, [name, n] =>
    println!(fact(string->number(n)))
  )
)