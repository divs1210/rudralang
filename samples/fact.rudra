ns!(fact,
  {} =>

  letfn fact := [n] =>
    cond(
      (n == 0) => 1,
      :else    => (n * fact((n - 1)))
    )

  defn!(main!, {}, [_, n] =>
    let n := string->number(n)
    println!(fact(n))
  )
)